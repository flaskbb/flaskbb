// Aurora Dark ships CSS only - all JavaScript is reused from the default
// Aurora theme (see templates/layout.html). This entry exists purely so the
// build has something to attach the extracted stylesheet to.
import "./scss/styles.scss";
